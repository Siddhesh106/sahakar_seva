{
  "framework": "vite",
  "buildCommand": "cd frontend-customer && npm install && npm run build",
  "outputDirectory": "frontend-customer/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
